i=0
for columns in ( raw.strip().split() for raw in c):
    if 

for line in open('ec_clustering_tree.txt') :
    if i!=0:
        i+=1
        columns = line.split('\t')
        ec1 = columns[1]
    
